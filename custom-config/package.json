{
  "name": "custom-config",
  "version": "1.0.0",
  "description": "Custom config, sharable config for multiple node app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nconf": "^0.9.1"
  }
}
